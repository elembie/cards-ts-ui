import { StateTypes, ApiStateTypes } from "./types"
import { ShdState } from './shd/types'

const mapApiState(state: ApiStateTypes): StateTypes => {

    switch(typeof(state)) {
        case shd.
    } 
}